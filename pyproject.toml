[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sampling-methods"
version = "0.1.0"
description = "Sampling methods for X-MACE transfer learning"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["sampling_methods","utils"]